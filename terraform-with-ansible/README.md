# Terraform with ansible

# Pre-Requisites:
    Install Terraform
    Install ansible
    Change mode of pem file to 400
# Run below commands
    terraform init
    terraform plan
    terraform apply
